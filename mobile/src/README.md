# Mobile Source

Suggested source layout:

- `app/`: app entry points and screens
- `components/`: reusable mobile UI pieces
- `features/`: ESS/MSS feature modules
- `lib/`: API client, auth, storage, notifications
- `navigation/`: tab and stack navigation setup
