# Shared Packages

This folder is reserved for code shared by the web and mobile apps.

Suggested packages:

- `ui`: shared design tokens and cross-platform patterns where practical
- `config`: shared TypeScript config and lint presets
- `types`: shared API and domain types
